@using DamaGame.Data.Models
@using DamaGame.Web.ViewModels.Games
@model GameViewModel

@{
    this.ViewData["Title"] = "Game";
}

<h1 class="card-title text-center text-primary">@this.ViewData["Title"]   triabva da se pravi</h1>

<h1 class="card-title text-center text-primary"> @this.Model.LeftPlayer.Name    &    @this.Model.RightPlayer.Name</h1>


@foreach (var position in this.Model.Playground.Positions)
{
    <p class="card-title text-center text-white"> @position.Figure ... @position.Color .... @position.StatePosition</p>
}
